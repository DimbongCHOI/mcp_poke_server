name: "@DimbongCHOI/mcp_poke_ver2"
version: "1.0.0"
description: "포켓몬 정보를 조회하고 검색할 수 있는 MCP 서버"
author: "DimbongCHOI"
license: "MIT"
categories:
  - "utilities"
  - "pokemon"
icon: "🎮"
homepage: "https://github.com/DimbongCHOI/mcp_poke_ver2"
repository: "https://github.com/DimbongCHOI/mcp_poke_ver2"

runtime: "container"

startCommand:
  type: "http"
  configSchema:
    type: "object"
    required:
      - api_key
    properties:
      api_key:
        type: "string"
        description: "API key required via ?api_key=... or Authorization header"
  exampleConfig:
    api_key: "demo-key"

build:
  dockerfile: "Dockerfile"
  dockerBuildPath: "."

env:
  NODE_ENV: "production"
  PORT: "8787"
